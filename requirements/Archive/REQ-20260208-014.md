# REQ-20260208-014 — Age Calculator (Time & Date)

## Status: COMPLETE

- Type: Calculator
- Change Type: SEO
- Priority: HIGH
- SEO Impact: YES
- Route (must remain live): `/time-and-date/age-calculator/`

## 1) Objective

Upgrade SEO + structured data for the existing Age Calculator page without changing calculation behavior or UI layout.

Constraints:
- Preserve calculation pane behavior and layout (UI-3.1, UI-3.2).
- Do not change compute logic unless required for correctness (non-goal for this REQ).

## 2) Scope

In-scope:
- Metadata: title, description, canonical (SEO_RULES P1.1–P1.4).
- Structured data: WebPage, SoftwareApplication, BreadcrumbList, FAQPage (SEO_RULES P2.*).

Out of scope:
- New modes/inputs/outputs.
- Any changes that introduce dropdowns (UI-2.5) or auto-recalculation on input edits (UI-2.6).

## 3) Requirements

### 3.1 Canonical + on-page SEO

- Canonical must be: `https://calchowmuch.com/time-and-date/age-calculator/` (SEO_RULES P1.3).
- Exactly one visible H1 containing the primary keyword (SEO_RULES P1.4).

### 3.2 Structured data

- Add/verify JSON-LD for:
  - WebPage
  - SoftwareApplication
  - BreadcrumbList
  - FAQPage (FAQs must exist and the JSON-LD must match the visible FAQs verbatim) (SEO_RULES P2.*).

## 4) Acceptance Criteria

- No visual/layout changes to calculation pane or shell (UI-3.1, UI-3.2).
- No input-edit auto recomputation introduced (UI-2.6).
- SEO automated validation for this route passes for P1/P2/P5.

## 5) Required Tests (Selection + Scope)

- SEO: run the scoped SEO suite for this route (SEO_RULES P1/P2/P5).
- E2E: only if any UI behavior is touched.

## 6) Evidence / Traceability

- Record SEO results in `requirements/compliance/seo_tracker.md` for `/time-and-date/age-calculator/`.
