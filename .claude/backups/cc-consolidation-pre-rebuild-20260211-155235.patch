diff --git a/public/calculators/loans/credit-card-consolidation/calculator.css b/public/calculators/loans/credit-card-consolidation/calculator.css
index 2c27904..21f2b3b 100644
--- a/public/calculators/loans/credit-card-consolidation/calculator.css
+++ b/public/calculators/loans/credit-card-consolidation/calculator.css
@@ -1,50 +1,364 @@
-/* Shared calculator styles live in /assets/css/calculator.css */
+/* Credit Card Consolidation - Repayment inspired premium UI */
 
-#calc-cc-con .cc-con-results-list {
+#calc-cc-con .cc-con-ui {
   display: grid;
-  gap: 6px;
+  gap: 1rem;
 }
 
-#calc-cc-con .result-line {
-  font-size: 16px;
-  font-weight: 600;
+#calc-cc-con .cc-con-hero {
+  position: relative;
+  display: grid;
+  grid-template-columns: minmax(0, 1.12fr) minmax(300px, 0.88fr);
+  gap: 1.45rem;
+  align-items: start;
+}
+
+#calc-cc-con .cc-con-form-panel,
+#calc-cc-con .cc-con-preview-panel {
+  position: relative;
+  border-radius: 1.1rem;
+  border: 1px solid rgba(125, 211, 252, 0.24);
+  background: linear-gradient(160deg, rgba(15, 23, 42, 0.95), rgba(30, 41, 59, 0.9));
+  padding: 1.1rem;
+  overflow: hidden;
+}
+
+#calc-cc-con .cc-con-form-panel::before,
+#calc-cc-con .cc-con-preview-panel::before {
+  content: '';
+  position: absolute;
+  inset: -45% auto auto -35%;
+  width: 220px;
+  height: 220px;
+  border-radius: 50%;
+  pointer-events: none;
+  background: radial-gradient(circle, rgba(59, 130, 246, 0.28), rgba(59, 130, 246, 0));
+}
+
+#calc-cc-con .cc-con-form-panel::after,
+#calc-cc-con .cc-con-preview-panel::after {
+  content: '';
+  position: absolute;
+  right: -70px;
+  bottom: -85px;
+  width: 210px;
+  height: 210px;
+  border-radius: 50%;
+  pointer-events: none;
+  background: radial-gradient(circle, rgba(251, 146, 60, 0.22), rgba(251, 146, 60, 0));
+}
+
+#calc-cc-con .cc-con-form-panel {
+  display: grid;
+  gap: 1.02rem;
+  box-shadow:
+    0 8px 26px rgba(2, 6, 23, 0.45),
+    0 0 44px rgba(59, 130, 246, 0.08);
+}
+
+#calc-cc-con .cc-con-form-panel .helper {
+  margin: 0;
+  font-size: 0.84rem;
+  line-height: 1.5;
+  color: rgba(226, 232, 240, 0.9);
+}
+
+#calc-cc-con .input-stack {
+  display: grid;
+  gap: 0.92rem;
+}
+
+#calc-cc-con .input-grid {
+  display: grid;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: 0.92rem;
+}
+
+#calc-cc-con .input-row {
+  margin: 0;
+  display: flex;
+  flex-direction: column;
+  gap: 0.34rem;
+  min-width: 0;
+}
+
+#calc-cc-con .input-row label {
+  font-size: 0.77rem;
+  font-weight: 700;
+  color: rgba(226, 232, 240, 0.96);
+  letter-spacing: 0.02em;
+}
+
+#calc-cc-con .input-row input[type='number'] {
+  width: 100%;
+  min-height: 40px;
+  border-radius: 0.78rem;
+  border: 1px solid rgba(148, 163, 184, 0.42);
+  background: rgba(15, 23, 42, 0.74);
+  color: rgba(248, 250, 252, 0.98);
+  padding: 0.42rem 0.72rem;
+  font-size: 0.88rem;
+  line-height: 1.25;
+  transition:
+    border-color 0.2s ease,
+    box-shadow 0.2s ease,
+    transform 0.2s ease;
+}
+
+#calc-cc-con .input-row input[type='number']:hover {
+  border-color: rgba(125, 211, 252, 0.62);
+}
+
+#calc-cc-con .input-row input[type='number']:focus-visible {
+  outline: 2px solid rgba(125, 211, 252, 0.62);
+  outline-offset: 1px;
+  border-color: rgba(56, 189, 248, 0.86);
+  box-shadow: 0 0 0 4px rgba(56, 189, 248, 0.12);
+  transform: translateY(-1px);
+}
+
+#calc-cc-con .cc-con-action-row {
+  margin-top: 0.2rem;
+}
+
+#calc-cc-con .cc-con-action-row .calculator-button {
+  min-height: 48px;
+  border-radius: 0.86rem;
+  border: 1px solid rgba(251, 191, 36, 0.62);
+  background: linear-gradient(130deg, rgba(249, 115, 22, 0.96), rgba(37, 99, 235, 0.9));
+  color: #fff;
+  font-size: 0.89rem;
+  font-weight: 700;
+  letter-spacing: 0.02em;
+  transition:
+    transform 0.18s ease,
+    box-shadow 0.25s ease,
+    filter 0.25s ease;
+}
+
+#calc-cc-con .cc-con-action-row .calculator-button:hover {
+  transform: translateY(-1px);
+  filter: saturate(1.06);
+  box-shadow:
+    0 10px 28px rgba(2, 132, 199, 0.42),
+    0 0 22px rgba(251, 146, 60, 0.2);
+}
+
+#calc-cc-con .cc-con-action-row .calculator-button:active {
+  transform: translateY(0);
+  box-shadow: 0 5px 14px rgba(2, 132, 199, 0.3);
+}
+
+#calc-cc-con .cc-con-preview-panel {
+  display: grid;
+  gap: 0.95rem;
+  align-content: start;
+  box-shadow:
+    0 10px 30px rgba(2, 6, 23, 0.5),
+    0 0 46px rgba(56, 189, 248, 0.09);
+}
+
+#calc-cc-con .cc-con-preview-main {
+  border-radius: 0.96rem;
+  border: 1px solid rgba(148, 163, 184, 0.18);
+  background: linear-gradient(165deg, rgba(15, 23, 42, 0.56), rgba(30, 64, 175, 0.17));
+  padding: 0.94rem;
+  display: grid;
+  gap: 0.58rem;
+}
+
+#calc-cc-con .cc-con-preview-kicker {
+  margin: 0;
+  font-size: 0.71rem;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  color: rgba(191, 219, 254, 0.95);
+  font-weight: 700;
+}
+
+#calc-cc-con #cc-con-result,
+#calc-cc-con #cc-con-summary {
+  margin: 0;
+  border: 0;
+  padding: 0;
+  background: transparent;
+}
+
+#calc-cc-con #cc-con-result {
+  min-height: 84px;
+  display: grid;
+  align-content: start;
+  gap: 0.5rem;
+}
+
+#calc-cc-con #cc-con-result .placeholder {
+  margin: 0;
+  border-radius: 0.7rem;
+  border: 1px dashed rgba(148, 163, 184, 0.5);
+  background: rgba(15, 23, 42, 0.42);
+  color: rgba(203, 213, 225, 0.9);
+  font-size: 0.79rem;
+  line-height: 1.45;
+  padding: 0.62rem 0.68rem;
 }
 
 #calc-cc-con .form-error {
   margin: 0;
-  font-size: 14px;
+  font-size: 0.8rem;
+  line-height: 1.4;
+  border-radius: 0.68rem;
+  border: 1px solid rgba(248, 113, 113, 0.62);
+  background: rgba(127, 29, 29, 0.45);
+  color: #fecaca;
+  padding: 0.52rem 0.64rem;
+}
+
+#calc-cc-con .cc-con-results-list {
+  display: grid;
+  gap: 0.42rem;
+}
+
+#calc-cc-con .cc-con-results-list .result-line {
+  margin: 0;
+  border-radius: 0.62rem;
+  border: 1px solid rgba(125, 211, 252, 0.22);
+  background: linear-gradient(140deg, rgba(15, 23, 42, 0.52), rgba(30, 41, 59, 0.42));
+  padding: 0.46rem 0.58rem;
+  color: rgba(224, 242, 254, 0.97);
+  font-size: 0.8rem;
   font-weight: 600;
-  color: #dc2626;
+  line-height: 1.38;
+  transition:
+    transform 0.2s ease,
+    border-color 0.25s ease,
+    box-shadow 0.25s ease;
+}
+
+#calc-cc-con .cc-con-results-list .result-line:hover {
+  transform: translateX(2px);
+  border-color: rgba(125, 211, 252, 0.52);
+  box-shadow: 0 6px 14px rgba(59, 130, 246, 0.14);
 }
 
 #calc-cc-con #cc-con-summary {
-  display: grid;
-  gap: 4px;
+  margin-top: 0.18rem;
+  color: rgba(191, 219, 254, 0.94);
+  font-size: 0.77rem;
+  line-height: 1.44;
 }
 
 #calc-cc-con #cc-con-summary p {
   margin: 0;
 }
 
+#calc-cc-con .cc-con-snapshot-list {
+  margin: 0;
+  padding: 0.22rem 0;
+  display: grid;
+  gap: 0.08rem;
+}
+
+#calc-cc-con .cc-con-snapshot-row {
+  display: flex;
+  justify-content: space-between;
+  align-items: baseline;
+  gap: 0.7rem;
+  border-bottom: 1px solid rgba(148, 163, 184, 0.12);
+  padding: 0.5rem 0;
+}
+
+#calc-cc-con .cc-con-snapshot-row:last-child {
+  border-bottom: 0;
+}
+
+#calc-cc-con .cc-con-snapshot-row span {
+  font-size: 0.74rem;
+  color: rgba(148, 163, 184, 0.84);
+}
+
+#calc-cc-con .cc-con-snapshot-row strong {
+  font-size: 0.84rem;
+  font-weight: 700;
+  color: rgba(248, 250, 252, 0.98);
+  text-align: right;
+  line-height: 1.3;
+  font-variant-numeric: tabular-nums;
+}
+
 #cc-con-explanation {
   display: grid;
-  gap: 10px;
+  gap: 0.92rem;
+}
+
+#cc-con-explanation h2 {
+  margin: 0;
+  font-size: 1.06rem;
+  font-weight: 700;
+  color: rgba(226, 232, 240, 0.97);
+}
+
+#cc-con-explanation h3 {
+  margin: 0;
+  font-size: 0.88rem;
+  font-weight: 700;
+  letter-spacing: 0.04em;
+  text-transform: uppercase;
+  color: rgba(191, 219, 254, 0.94);
+  border-bottom: 0;
+  padding-bottom: 0;
+}
+
+#cc-con-explanation h3::after {
+  content: none;
 }
 
 #cc-con-explanation > p {
   margin: 0;
-  line-height: 1.45;
+  font-size: 0.84rem;
+  line-height: 1.56;
+  color: rgba(203, 213, 225, 0.92);
 }
 
 #cc-con-explanation [data-cc-con] {
-  font-weight: 600;
+  font-weight: 700;
   display: inline-block;
-  padding: 1px 5px;
-  border-radius: 6px;
-  line-height: 1.2;
+  padding: 1px 6px;
+  border-radius: 8px;
+  line-height: 1.22;
   white-space: nowrap;
-  background: rgba(59, 130, 246, 0.24);
-  outline: 1px solid rgba(59, 130, 246, 0.45);
+  background: rgba(59, 130, 246, 0.45);
+  outline: 1px solid rgba(59, 130, 246, 0.65);
+  color: rgba(226, 232, 240, 0.99);
+}
+
+#cc-con-explanation .table-scroll {
+  max-height: 326px;
+  overflow: auto;
+  border-radius: 0.8rem;
+  border: 1px solid rgba(148, 163, 184, 0.3);
+  background: rgba(15, 23, 42, 0.62);
+}
+
+#cc-con-explanation .calculator-table {
+  min-width: 420px;
+  border-collapse: collapse;
+}
+
+#cc-con-explanation .calculator-table thead th {
+  position: sticky;
+  top: 0;
+  z-index: 2;
+  background: rgba(30, 41, 59, 0.98);
+  color: #fde68a;
+  font-size: 0.73rem;
+  letter-spacing: 0.04em;
+  text-transform: uppercase;
+}
+
+#cc-con-explanation .calculator-table th,
+#cc-con-explanation .calculator-table td {
+  border: 1px solid rgba(148, 163, 184, 0.24);
+  padding: 0.5rem;
 }
 
 #cc-con-explanation #cc-con-scenario-table th:nth-child(3),
@@ -59,6 +373,16 @@
   text-align: right;
 }
 
+#cc-con-explanation .calculator-table td {
+  font-size: 0.79rem;
+  color: rgba(226, 232, 240, 0.95);
+  white-space: nowrap;
+}
+
+#cc-con-explanation .calculator-table tbody tr:nth-child(even) {
+  background: rgba(30, 41, 59, 0.34);
+}
+
 #cc-con-explanation .cc-con-table-placeholder-row td {
   text-align: center;
   font-style: italic;
@@ -66,9 +390,35 @@
 }
 
 #cc-con-explanation .cc-con-faq-item {
-  border: 1px solid var(--border-color);
+  border: 1px solid rgba(148, 163, 184, 0.16);
+  border-radius: 0.78rem;
+  border-top: 2px solid rgba(245, 158, 11, 0.46);
+  background: linear-gradient(160deg, rgba(15, 23, 42, 0.45), rgba(30, 41, 59, 0.36));
 }
 
 #cc-con-explanation .cc-con-faq-item .faq-answer {
-  margin-left: 12px;
+  margin-left: 0.85rem;
+}
+
+@media (max-width: 1220px) {
+  #calc-cc-con .cc-con-hero {
+    grid-template-columns: 1fr;
+  }
+}
+
+@media (max-width: 900px) {
+  #calc-cc-con .input-grid {
+    grid-template-columns: 1fr;
+  }
+
+  #cc-con-explanation .calculator-table {
+    min-width: 360px;
+  }
+}
+
+@media (max-width: 720px) {
+  #calc-cc-con .cc-con-form-panel,
+  #calc-cc-con .cc-con-preview-panel {
+    padding: 0.92rem;
+  }
 }
diff --git a/public/calculators/loans/credit-card-consolidation/index.html b/public/calculators/loans/credit-card-consolidation/index.html
index f7036fc..7f7fcc8 100644
--- a/public/calculators/loans/credit-card-consolidation/index.html
+++ b/public/calculators/loans/credit-card-consolidation/index.html
@@ -1,54 +1,140 @@
 <div id="calc-cc-con">
-  <div class="calculator-ui">
-    <p class="helper">
-      Compare paying your current credit cards separately versus consolidating into a fixed-rate loan.
-    </p>
-
-    <div class="input-stack">
-      <div class="input-row">
-        <label for="cc-con-balance">Total Card Balance</label>
-        <input id="cc-con-balance" type="number" value="12000" min="0.01" step="0.01" inputmode="decimal" />
-      </div>
+  <div class="calculator-ui cc-con-ui">
+    <section class="cc-con-hero" aria-label="Credit card consolidation planner">
+      <div class="cc-con-form-panel">
+        <p class="helper">
+          Compare your current card payoff with a consolidation loan in one smooth, always-visible
+          dashboard.
+        </p>
 
-      <div class="input-grid">
-        <div class="input-row">
-          <label for="cc-con-apr">Current Average APR (%)</label>
-          <input id="cc-con-apr" type="number" value="19.5" min="0" step="0.01" inputmode="decimal" />
-        </div>
+        <div class="input-stack">
+          <div class="input-row">
+            <label for="cc-con-balance">Total Card Balance</label>
+            <input
+              id="cc-con-balance"
+              type="number"
+              value="12000"
+              min="0.01"
+              step="0.01"
+              inputmode="decimal"
+            />
+          </div>
 
-        <div class="input-row">
-          <label for="cc-con-payment">Current Monthly Payment</label>
-          <input id="cc-con-payment" type="number" value="400" min="0.01" step="0.01" inputmode="decimal" />
-        </div>
-      </div>
+          <div class="input-grid">
+            <div class="input-row">
+              <label for="cc-con-apr">Current Average APR (%)</label>
+              <input
+                id="cc-con-apr"
+                type="number"
+                value="19.5"
+                min="0"
+                step="0.01"
+                inputmode="decimal"
+              />
+            </div>
+
+            <div class="input-row">
+              <label for="cc-con-payment">Current Monthly Payment</label>
+              <input
+                id="cc-con-payment"
+                type="number"
+                value="400"
+                min="0.01"
+                step="0.01"
+                inputmode="decimal"
+              />
+            </div>
+          </div>
 
-      <div class="input-grid">
-        <div class="input-row">
-          <label for="cc-con-new-apr">Consolidation APR (%)</label>
-          <input id="cc-con-new-apr" type="number" value="10.5" min="0" step="0.01" inputmode="decimal" />
+          <div class="input-grid">
+            <div class="input-row">
+              <label for="cc-con-new-apr">Consolidation APR (%)</label>
+              <input
+                id="cc-con-new-apr"
+                type="number"
+                value="10.5"
+                min="0"
+                step="0.01"
+                inputmode="decimal"
+              />
+            </div>
+
+            <div class="input-row">
+              <label for="cc-con-term">Consolidation Term (years)</label>
+              <input
+                id="cc-con-term"
+                type="number"
+                value="3"
+                min="1"
+                step="1"
+                inputmode="decimal"
+              />
+            </div>
+          </div>
+
+          <div class="input-row">
+            <label for="cc-con-fees">Consolidation Fees (optional)</label>
+            <input
+              id="cc-con-fees"
+              type="number"
+              value="250"
+              min="0"
+              step="0.01"
+              inputmode="decimal"
+            />
+          </div>
         </div>
 
-        <div class="input-row">
-          <label for="cc-con-term">Consolidation Term (years)</label>
-          <input id="cc-con-term" type="number" value="3" min="1" step="1" inputmode="decimal" />
+        <div class="cc-con-action-row">
+          <button class="calculator-button full-width" type="button" id="cc-con-calc">
+            Calculate Consolidation Plan
+          </button>
         </div>
       </div>
 
-      <div class="input-row">
-        <label for="cc-con-fees">Consolidation Fees (optional)</label>
-        <input id="cc-con-fees" type="number" value="250" min="0" step="0.01" inputmode="decimal" />
-      </div>
-    </div>
+      <aside class="cc-con-preview-panel" aria-label="Consolidation snapshot">
+        <div class="cc-con-preview-main">
+          <p class="cc-con-preview-kicker">Projected consolidation outcome</p>
 
-    <button class="calculator-button full-width" type="button" id="cc-con-calc">Calculate</button>
+          <div class="result" id="cc-con-result" aria-live="polite">
+            <p class="placeholder" id="cc-con-placeholder">Enter values and click Calculate.</p>
+            <p class="form-error is-hidden" id="cc-con-error"></p>
+            <div class="cc-con-results-list is-hidden" id="cc-con-results-list"></div>
+          </div>
 
-    <div class="result" id="cc-con-result" aria-live="polite">
-      <p class="placeholder" id="cc-con-placeholder">Enter values and click Calculate.</p>
-      <p class="form-error is-hidden" id="cc-con-error"></p>
-      <div class="cc-con-results-list is-hidden" id="cc-con-results-list"></div>
-    </div>
+          <div class="result-detail" id="cc-con-summary" aria-live="polite">
+            <p>Update inputs and click Calculate to refresh this comparison.</p>
+          </div>
+        </div>
 
-    <div class="result-detail is-hidden" id="cc-con-summary" aria-live="polite"></div>
+        <div class="cc-con-snapshot-list">
+          <div class="cc-con-snapshot-row">
+            <span>Current Payoff</span>
+            <strong data-cc-con="current-months">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Consolidation Payment</span>
+            <strong data-cc-con="new-payment">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Monthly Difference</span>
+            <strong data-cc-con="monthly-diff">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Interest Difference</span>
+            <strong data-cc-con="interest-diff">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Total Cost Difference</span>
+            <strong data-cc-con="total-diff">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Recommendation</span>
+            <strong data-cc-con="recommendation">—</strong>
+          </div>
+        </div>
+      </aside>
+    </section>
   </div>
 </div>
 
diff --git a/public/calculators/loans/credit-card-consolidation/module.js b/public/calculators/loans/credit-card-consolidation/module.js
index ccc4fea..13fd9db 100644
--- a/public/calculators/loans/credit-card-consolidation/module.js
+++ b/public/calculators/loans/credit-card-consolidation/module.js
@@ -16,29 +16,17 @@ const resultsList = document.querySelector('#cc-con-results-list');
 const summaryDiv = document.querySelector('#cc-con-summary');
 const tableBody = document.querySelector('#cc-con-table-body');
 
-const explanationSpans = Array.from(document.querySelectorAll('[data-cc-con]')).reduce((acc, el) => {
-  const key = el.dataset.ccCon;
-  if (!acc[key]) {
-    acc[key] = [];
-  }
-  acc[key].push(el);
-  return acc;
-}, {});
-
-const INPUT_KEYS = [
-  'balance',
-  'current-apr',
-  'current-payment',
-  'new-apr',
-  'new-term',
-  'fees',
-  'scenario-balance',
-  'scenario-current-apr',
-  'scenario-current-payment',
-  'scenario-new-apr',
-  'scenario-new-term',
-  'scenario-fees',
-];
+const explanationSpans = Array.from(document.querySelectorAll('[data-cc-con]')).reduce(
+  (acc, el) => {
+    const key = el.dataset.ccCon;
+    if (!acc[key]) {
+      acc[key] = [];
+    }
+    acc[key].push(el);
+    return acc;
+  },
+  {}
+);
 
 const OUTPUT_KEYS = [
   'current-months',
@@ -283,26 +271,57 @@ function clearError() {
   errorMessage.classList.add('is-hidden');
 }
 
+function showProjectionResults() {
+  clearError();
+  placeholder?.classList.add('is-hidden');
+  resultsList?.classList.remove('is-hidden');
+  summaryDiv?.classList.remove('is-hidden');
+}
+
+function showProjectionPending() {
+  clearError();
+  placeholder?.classList.remove('is-hidden');
+  resultsList?.classList.add('is-hidden');
+  summaryDiv?.classList.add('is-hidden');
+}
+
 function setOutputPlaceholders() {
   OUTPUT_KEYS.forEach((key) => setSpan(key, '—'));
 }
 
 function setInputSpans(values, termMonths) {
-  const termLabel = formatMonths(termMonths);
-
-  setSpan('balance', formatAmount(values.balance));
-  setSpan('current-apr', formatPercent(values.currentApr));
-  setSpan('current-payment', formatAmount(values.currentPayment));
-  setSpan('new-apr', formatPercent(values.consolidationApr));
+  const resolvedTermMonths =
+    Number.isFinite(termMonths) && termMonths > 0
+      ? termMonths
+      : Number.isFinite(values.termYears) && values.termYears > 0
+        ? Math.max(1, Math.round(values.termYears * 12))
+        : null;
+  const termLabel = resolvedTermMonths ? formatMonths(resolvedTermMonths) : '—';
+  const balanceLabel = Number.isFinite(values.balance) ? formatAmount(values.balance) : '—';
+  const currentAprLabel = Number.isFinite(values.currentApr)
+    ? formatPercent(values.currentApr)
+    : '—';
+  const currentPaymentLabel = Number.isFinite(values.currentPayment)
+    ? formatAmount(values.currentPayment)
+    : '—';
+  const newAprLabel = Number.isFinite(values.consolidationApr)
+    ? formatPercent(values.consolidationApr)
+    : '—';
+  const feesLabel = Number.isFinite(values.fees) ? formatAmount(values.fees) : '—';
+
+  setSpan('balance', balanceLabel);
+  setSpan('current-apr', currentAprLabel);
+  setSpan('current-payment', currentPaymentLabel);
+  setSpan('new-apr', newAprLabel);
   setSpan('new-term', termLabel);
-  setSpan('fees', formatAmount(values.fees));
+  setSpan('fees', feesLabel);
 
-  setSpan('scenario-balance', formatAmount(values.balance));
-  setSpan('scenario-current-apr', formatPercent(values.currentApr));
-  setSpan('scenario-current-payment', formatAmount(values.currentPayment));
-  setSpan('scenario-new-apr', formatPercent(values.consolidationApr));
+  setSpan('scenario-balance', balanceLabel);
+  setSpan('scenario-current-apr', currentAprLabel);
+  setSpan('scenario-current-payment', currentPaymentLabel);
+  setSpan('scenario-new-apr', newAprLabel);
   setSpan('scenario-new-term', termLabel);
-  setSpan('scenario-fees', formatAmount(values.fees));
+  setSpan('scenario-fees', feesLabel);
 }
 
 function getRecommendation(data) {
@@ -385,22 +404,6 @@ function updateTable(data) {
   `;
 }
 
-function showPlaceholder() {
-  clearError();
-  placeholder?.classList.remove('is-hidden');
-  resultsList?.classList.add('is-hidden');
-  summaryDiv?.classList.add('is-hidden');
-
-  if (resultsList) {
-    resultsList.innerHTML = '';
-  }
-  if (summaryDiv) {
-    summaryDiv.innerHTML = '';
-  }
-
-  updateTable(null);
-}
-
 function showError(message) {
   if (errorMessage) {
     errorMessage.textContent = message;
@@ -461,7 +464,9 @@ function calculate() {
         ? 'higher'
         : 'unchanged';
 
-  addResultLine(`Monthly payment change: ${formatSignedAmount(data.monthlyDifference)} (${monthlyDirection})`);
+  addResultLine(
+    `Monthly payment change: ${formatSignedAmount(data.monthlyDifference)} (${monthlyDirection})`
+  );
   addResultLine(`Current payoff time: ${formatMonths(data.current.months)}`);
 
   if (summaryDiv) {
@@ -471,10 +476,7 @@ function calculate() {
       `<p><strong>Consolidation payment:</strong> ${formatCurrency(data.consolidation.monthlyPayment)}</p>`;
   }
 
-  clearError();
-  placeholder?.classList.add('is-hidden');
-  resultsList?.classList.remove('is-hidden');
-  summaryDiv?.classList.remove('is-hidden');
+  showProjectionResults();
 
   updateTable(data);
 }
@@ -484,8 +486,10 @@ function resetAfterInputChange() {
     return;
   }
 
+  setInputSpans(readInputs());
   setOutputPlaceholders();
-  showPlaceholder();
+  updateTable(null);
+  showProjectionPending();
 }
 
 calculateButton?.addEventListener('click', () => {
@@ -497,37 +501,7 @@ document.querySelectorAll('#calc-cc-con input').forEach((input) => {
   input.addEventListener('input', resetAfterInputChange);
 });
 
-(function initializeExplanation() {
-  const values = readInputs();
-  const validationError = validateInputs(values);
-
-  setOutputPlaceholders();
-  placeholder?.classList.remove('is-hidden');
-  resultsList?.classList.add('is-hidden');
-  summaryDiv?.classList.add('is-hidden');
-
-  if (resultsList) {
-    resultsList.innerHTML = '';
-  }
-  if (summaryDiv) {
-    summaryDiv.innerHTML = '';
-  }
-
-  if (validationError) {
-    INPUT_KEYS.forEach((key) => setSpan(key, '—'));
-    updateTable(null);
-    return;
-  }
-
-  const { params, termMonths } = toCalcParams(values);
-  const data = calculateConsolidation(params);
-  if (data.error) {
-    INPUT_KEYS.forEach((key) => setSpan(key, '—'));
-    updateTable(null);
-    return;
-  }
-
-  setInputSpans(values, termMonths);
-  setOutputSpans(data);
-  updateTable(data);
+(function initializeDefaultOutcome() {
+  hasCalculated = true;
+  calculate();
 })();
diff --git a/public/loans/credit-card-consolidation/index.html b/public/loans/credit-card-consolidation/index.html
index ec8871f..8516582 100644
--- a/public/loans/credit-card-consolidation/index.html
+++ b/public/loans/credit-card-consolidation/index.html
@@ -92,58 +92,137 @@
         </aside>
         <section class="center-column">
           <div class="panel panel-scroll">
-  <h1 id="calculator-title">Card Consolidation</h1>
+  <h1 id="calculator-title">Credit Card Consolidation Calculator</h1>
   <div id="calc-cc-con">
-  <div class="calculator-ui">
-    <p class="helper">
-      Compare paying your current credit cards separately versus consolidating into a fixed-rate loan.
-    </p>
+  <div class="calculator-ui cc-con-ui">
+    <section class="cc-con-hero" aria-label="Credit card consolidation planner">
+      <div class="cc-con-form-panel">
+        <p class="helper">
+          Compare your current card payoff with a consolidation loan in one smooth, always-visible
+          dashboard.
+        </p>
 
-    <div class="input-stack">
-      <div class="input-row">
-        <label for="cc-con-balance">Total Card Balance</label>
-        <input id="cc-con-balance" type="number" value="12000" min="0.01" step="0.01" inputmode="decimal" />
-      </div>
+        <div class="input-stack">
+          <div class="input-row">
+            <label for="cc-con-balance">Total Card Balance</label>
+            <input
+              id="cc-con-balance"
+              type="number"
+              value="12000"
+              min="0.01"
+              step="0.01"
+              inputmode="decimal"
+            />
+          </div>
 
-      <div class="input-grid">
-        <div class="input-row">
-          <label for="cc-con-apr">Current Average APR (%)</label>
-          <input id="cc-con-apr" type="number" value="19.5" min="0" step="0.01" inputmode="decimal" />
-        </div>
+          <div class="input-grid">
+            <div class="input-row">
+              <label for="cc-con-apr">Current Average APR (%)</label>
+              <input
+                id="cc-con-apr"
+                type="number"
+                value="19.5"
+                min="0"
+                step="0.01"
+                inputmode="decimal"
+              />
+            </div>
 
-        <div class="input-row">
-          <label for="cc-con-payment">Current Monthly Payment</label>
-          <input id="cc-con-payment" type="number" value="400" min="0.01" step="0.01" inputmode="decimal" />
-        </div>
-      </div>
+            <div class="input-row">
+              <label for="cc-con-payment">Current Monthly Payment</label>
+              <input
+                id="cc-con-payment"
+                type="number"
+                value="400"
+                min="0.01"
+                step="0.01"
+                inputmode="decimal"
+              />
+            </div>
+          </div>
+
+          <div class="input-grid">
+            <div class="input-row">
+              <label for="cc-con-new-apr">Consolidation APR (%)</label>
+              <input
+                id="cc-con-new-apr"
+                type="number"
+                value="10.5"
+                min="0"
+                step="0.01"
+                inputmode="decimal"
+              />
+            </div>
 
-      <div class="input-grid">
-        <div class="input-row">
-          <label for="cc-con-new-apr">Consolidation APR (%)</label>
-          <input id="cc-con-new-apr" type="number" value="10.5" min="0" step="0.01" inputmode="decimal" />
+            <div class="input-row">
+              <label for="cc-con-term">Consolidation Term (years)</label>
+              <input id="cc-con-term" type="number" value="3" min="1" step="1" inputmode="decimal" />
+            </div>
+          </div>
+
+          <div class="input-row">
+            <label for="cc-con-fees">Consolidation Fees (optional)</label>
+            <input
+              id="cc-con-fees"
+              type="number"
+              value="250"
+              min="0"
+              step="0.01"
+              inputmode="decimal"
+            />
+          </div>
         </div>
 
-        <div class="input-row">
-          <label for="cc-con-term">Consolidation Term (years)</label>
-          <input id="cc-con-term" type="number" value="3" min="1" step="1" inputmode="decimal" />
+        <div class="cc-con-action-row">
+          <button class="calculator-button full-width" type="button" id="cc-con-calc">
+            Calculate Consolidation Plan
+          </button>
         </div>
       </div>
 
-      <div class="input-row">
-        <label for="cc-con-fees">Consolidation Fees (optional)</label>
-        <input id="cc-con-fees" type="number" value="250" min="0" step="0.01" inputmode="decimal" />
-      </div>
-    </div>
+      <aside class="cc-con-preview-panel" aria-label="Consolidation snapshot">
+        <div class="cc-con-preview-main">
+          <p class="cc-con-preview-kicker">Projected consolidation outcome</p>
 
-    <button class="calculator-button full-width" type="button" id="cc-con-calc">Calculate</button>
+          <div class="result" id="cc-con-result" aria-live="polite">
+            <p class="placeholder" id="cc-con-placeholder">Enter values and click Calculate.</p>
+            <p class="form-error is-hidden" id="cc-con-error"></p>
+            <div class="cc-con-results-list is-hidden" id="cc-con-results-list"></div>
+          </div>
 
-    <div class="result" id="cc-con-result" aria-live="polite">
-      <p class="placeholder" id="cc-con-placeholder">Enter values and click Calculate.</p>
-      <p class="form-error is-hidden" id="cc-con-error"></p>
-      <div class="cc-con-results-list is-hidden" id="cc-con-results-list"></div>
-    </div>
+          <div class="result-detail" id="cc-con-summary" aria-live="polite">
+            <p>Update inputs and click Calculate to refresh this comparison.</p>
+          </div>
+        </div>
 
-    <div class="result-detail is-hidden" id="cc-con-summary" aria-live="polite"></div>
+        <div class="cc-con-snapshot-list">
+          <div class="cc-con-snapshot-row">
+            <span>Current Payoff</span>
+            <strong data-cc-con="current-months">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Consolidation Payment</span>
+            <strong data-cc-con="new-payment">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Monthly Difference</span>
+            <strong data-cc-con="monthly-diff">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Interest Difference</span>
+            <strong data-cc-con="interest-diff">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Total Cost Difference</span>
+            <strong data-cc-con="total-diff">—</strong>
+          </div>
+          <div class="cc-con-snapshot-row">
+            <span>Recommendation</span>
+            <strong data-cc-con="recommendation">—</strong>
+          </div>
+        </div>
+      </aside>
+    </section>
   </div>
 </div>
 
diff --git a/requirements/specs/e2e/credit-card-consolidation.spec.js b/requirements/specs/e2e/credit-card-consolidation.spec.js
index 0d11db5..f4e8b93 100644
--- a/requirements/specs/e2e/credit-card-consolidation.spec.js
+++ b/requirements/specs/e2e/credit-card-consolidation.spec.js
@@ -6,7 +6,9 @@ test.describe('Credit Card Consolidation Calculator', () => {
   }) => {
     await page.goto('/loans/credit-card-consolidation');
 
-    await expect(page.locator('#calculator-title')).toHaveText('Credit Card Consolidation Calculator');
+    await expect(page.locator('#calculator-title')).toHaveText(
+      'Credit Card Consolidation Calculator'
+    );
 
     const topNavActive = page.locator('.top-nav .top-nav-link.is-active');
     await expect(topNavActive).toContainText('Credit Card');
@@ -59,7 +61,9 @@ test.describe('Credit Card Consolidation Calculator', () => {
     expect(Number.parseFloat(chipStyles.paddingLeft)).toBeGreaterThan(0);
   });
 
-  test('CONSOLIDATION-TEST-E2E-2: input change resets outputs until calculate click', async ({ page }) => {
+  test('CONSOLIDATION-TEST-E2E-2: input change resets projection until recalculate', async ({
+    page,
+  }) => {
     await page.goto('/loans/credit-card-consolidation');
 
     await page.locator('#cc-con-calc').click();
@@ -67,7 +71,7 @@ test.describe('Credit Card Consolidation Calculator', () => {
 
     await page.locator('#cc-con-payment').fill('450');
 
-    await expect(page.locator('#cc-con-placeholder')).toBeVisible();
+    await expect(page.locator('#cc-con-placeholder')).not.toHaveClass(/is-hidden/);
     await expect(page.locator('#cc-con-results-list')).toHaveClass(/is-hidden/);
     await expect(page.locator('#cc-con-summary')).toHaveClass(/is-hidden/);
     await expect(page.locator('[data-cc-con="current-months"]').first()).toHaveText('—');
